import React from 'react';
import { PropTypes } from 'helpers/react';
const { bool } = PropTypes;

const Footer = (props = {}) => {
  const { loading } = props;

  return (
    <div>
      FOOTER
    </div>
  );
};

export default Footer;
